import HTMLClass from '../../HTML'
import { Library } from '../../types'

const HTML: Library = {
  config: {
    name: 'HTML',
    type: 'library',
    targetVer: '1.0.0-indev.0'
  },
  init: (l, k, p) => {},
  data: HTMLClass
}

export default HTML
